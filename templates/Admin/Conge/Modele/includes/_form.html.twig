<div class="md-form mb-5">
    {{ form_widget(form.nom, {
        'attr' : {
            'required' : 'required',
            'class' : 'form-control',
            'autocomplete' : 'off'
        }
    }) }}
    {{ form_label(form.nom) }}
</div>
<div class="form-row">
    <div class="form-group mb-5">
        {{ form_label(form.type) }}
        <br>
        {% for child in form.type %}
            <div class="custom-control custom-radio custom-control-inline">
                {{ form_widget(child, { 'attr': {'class': 'custom-control-input'}}) }}
                {{ form_label(child,null,{
                    'label_attr' : {
                        'class' : 'custom-control-label'
                    }
                }) }}
            </div>
        {% endfor %}
    </div>
</div>
<div class="form-row">
    <div class="col">
        <div class="form-group mb-5">
            {{ form_label(form.service) }}
            <br>
            {% for child in form.service %}
                <div class="custom-control custom-radio custom-control-inline">
                    {{ form_widget(child, { 'attr': {'class': 'custom-control-input'}}) }}
                    {{ form_label(child,null,{
                        'label_attr' : {
                            'class' : 'custom-control-label'
                        }
                    }) }}
                </div>
            {% endfor %}
        </div>
    </div>
    <div class="col">
        <div class="form-group">
            {{ form_label(form.departement) }}
            <br>
            {% for child in form.departement %}
                <div class="custom-control custom-radio custom-control-inline">
                    {{ form_widget(child, { 'attr': {'class': 'custom-control-input'}}) }}
                    {{ form_label(child,null,{
                        'label_attr' : {
                            'class' : 'custom-control-label'
                        }
                    }) }}
                </div>
            {% endfor %}
        </div>
    </div>
    <div class="col">
        <div class="form-group">
            {{ form_label(form.direction) }}
            <br>
            {% for child in form.direction %}
                <div class="custom-control custom-radio custom-control-inline">
                    {{ form_widget(child, { 'attr': {'class': 'custom-control-input'}}) }}
                    {{ form_label(child,null,{
                        'label_attr' : {
                            'class' : 'custom-control-label'
                        }
                    }) }}
                </div>
            {% endfor %}
        </div>
    </div>
</div>

<div class="form-row">
    <div class="col">
        <div class="form-group mb-5">
            {{ form_label(form.justificatif) }}
            <br>
            {% for child in form.justificatif %}
                <div class="custom-control custom-radio custom-control-inline">
                    {{ form_widget(child, { 'attr': {'class': 'custom-control-input'}}) }}
                    {{ form_label(child,null,{
                        'label_attr' : {
                            'class' : 'custom-control-label'
                        }
                    }) }}
                </div>
            {% endfor %}
        </div>
    </div>
    <div class="col">
        <div class="form-group mb-5">
            {{ form_label(form.fixe) }}
            <br>
            {% for child in form.fixe %}
                <div class="custom-control custom-radio custom-control-inline">
                    {{ form_widget(child, { 'attr': {'class': 'custom-control-input'}}) }}
                    {{ form_label(child,null,{
                        'label_attr' : {
                            'class' : 'custom-control-label'
                        }
                    }) }}
                </div>
            {% endfor %}
        </div>
    </div>
</div>

<div class="form-row">
    {{ form_label(form.delaimin,null,{
        'label_attr' : {
            'class' : 'my-4'
        }
    }) }}
    <br>
    {% for child in form.delaimin %}
        <div class="form-group mb-5 mx-2">
            {{ form_label(child,null,{
                'label_attr':{
                    'class':'control-label'
                }
            }) }}
            {{ form_widget(child,{
                'attr':{
                    'class':'form-control'
                }
            }) }}
        </div>
    {% endfor %}
</div>

<div class="form-row">
    {{ form_label(form.delaimax,null,{
        'label_attr' : {
            'class' : 'my-4'
        }
    }) }}
    <br>
    {% for child in form.delaimax %}
        <div class="form-group mb-5 mx-2">
            {{ form_label(child,null,{
                'label_attr':{
                    'class':'control-label'
                }
            }) }}
            {{ form_widget(child,{
                'attr':{
                    'class':'form-control'
                }
            }) }}
        </div>
    {% endfor %}
</div>

<div class="form-group mb-5">
    {{ form_label(form.statuts) }}
    <br>
    {{ form_widget(form.statuts,{
        'attr':{
            'class':'chosen-select',
            'data-placeholder':'app.cmodele.forms.fields.statuts.placeholder'|trans
        }
    }) }}
</div>

<div class="form-row">
    <div class="form-group mb-5">
        {% for child in form.etat %}
            <div class="custom-control custom-radio custom-control-inline">
                {{ form_widget(child, { 'attr': {'class': 'custom-control-input'}}) }}
                {{ form_label(child,null,{
                    'label_attr' : {
                        'class' : 'custom-control-label'
                    }
                }) }}
            </div>
        {% endfor %}
    </div>
</div>

