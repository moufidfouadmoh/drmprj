{% extends 'Global/base.html.twig' %}
{% block styles %}
    <link rel="stylesheet" href="{{ asset('build/css/chosen.css') }}">
    {#<style>
        .number-style{
            padding-left: 15px;
            padding-right: 15px;
        }
    </style>#}
{% endblock %}
{% block body %}
    <div class="container">
        <div class="row">
            <div class="offset-1 col-10">
                <div class="card">
                    <h5 class="card-header light-blue lighten-1 white-text text-uppercase text-center">
                        <i class="lnr lnr-license"></i>
                        {{ 'app.cmodele.edit.modal.header'|trans }}
                    </h5>
                    {{ form_start(form,{
                        'attr':{
                            'class' : 'form form-horizontal form-bordered'
                        }
                    }) }}
                    <div class="card-content collpase show">
                        <div class="card-body px-lg-5 pt-0">
                            <div class="card-text">
                                <p>Lorem ipsum dolor sit amet, consectetur adipisicing elit. Aspernatur deserunt enim error facere libero molestiae numquam reiciendis sapiente sequi tenetur!</p>
                            </div>

                            {% include 'Admin/Conge/Modele/includes/_form.html.twig' %}
                        </div>
                        <div class="card-footer">
                            <button class="btn btn-success btn-rounded mb-4 float-right">
                                {{ 'save.title'|trans }}
                            </button>
                        </div>
                    </div>
                    {{ form_end(form) }}
                </div>
            </div>
        </div>
    </div>
{% endblock %}

{% block scripts %}
    <script type="text/javascript" src="{{ asset('build/js/jquery.form.min.js') }}"></script>
    <script type="text/javascript" src="{{ asset('build/js/chosen.jquery.js') }}"></script>
    <script type="text/javascript">
        $(document).ready(function () {
            $('#c_modele_form_statuts').chosen({

            });

            {#$('#c_modele_form_delai_years').number({

            });

            $('#c_modele_form_delai_months').number({

            });

            $('#c_modele_form_delai_days').number({

            });

            $('#c_modele_form_delai_hours').number({

            });#}
        });
    </script>
{% endblock %}