<?php

namespace App\Form\Search;

use App\Entity\Equipement;
use App\Entity\Includes\Search\MaterielMobilierSearch;
use App\Entity\MaterielMobilier;
use App\Repository\EquipementRepository;
use Symfony\Bridge\Doctrine\Form\Type\EntityType;
use Symfony\Component\Form\AbstractType;
use Symfony\Component\Form\FormBuilderInterface;
use Symfony\Component\OptionsResolver\OptionsResolver;

class MaterielMobilierSearchForm extends AbstractType
{
    public function buildForm(FormBuilderInterface $builder, array $options)
    {
        $builder
            ->add('equipements',EntityType::class,[
                'required' => false,
                'label' => false,
                'class' => Equipement::class,
                'query_builder' => function(EquipementRepository $repository){
                    $qb= $repository->createQueryBuilder('equipement');
                    $qb
                        ->leftJoin('equipement.materiels','materiels')
                        ->addSelect('materiels')
                        ->andWhere(
                            $qb->expr()->isInstanceOf('materiels',MaterielMobilier::class)
                        )
                    ;
                    return $qb;
                },
                'choice_label' => 'nom',
                'choice_value' => 'nom',
                'multiple' => true
            ])
        ;
    }

    public function configureOptions(OptionsResolver $resolver)
    {
        $resolver->setDefaults([
            'data_class' => MaterielMobilierSearch::class,
            'method' => 'GET',
            'csrf_protection' => false
        ]);
    }

    public function getBlockPrefix()
    {
        return ''; // TODO: Change the autogenerated stub
    }
}